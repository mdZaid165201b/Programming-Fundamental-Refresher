// Find Volume of sphere

let findVolumeOfSphere = (radius) => {
    let temp = (4/3)*3.14*radius*radius*radius;
    console.log(temp.toFixed(2));
    // console.log((4/3)*3.14*radius*radius*radius);
};

findVolumeOfSphere(2);
